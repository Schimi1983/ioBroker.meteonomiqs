// Prettier settings for @iobroker/eslint-config (W0076).
// The field tables in src/lib/fields.ts are deliberately one row per line,
// which is why printWidth is generous.
export default {
    semi: true,
    trailingComma: 'all',
    singleQuote: true,
    printWidth: 250,
    useTabs: false,
    tabWidth: 4,
    endOfLine: 'lf',
    arrowParens: 'always',
    bracketSpacing: true,
};
